# Which widget app to develop and build.
# Valid values: collection-search | spotify | placeholder
# Defaults to collection-search if not set.
VITE_WIDGET_APP=collection-search

# Required for the Spotify app — URL of your hosted Express server.
# Leave unset when using the collection-search app.
# VITE_API_BASE_URL=https://my-server.railway.app
